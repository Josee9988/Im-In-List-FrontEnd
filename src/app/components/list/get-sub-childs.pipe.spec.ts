import { GetSubChildsPipe } from './get-sub-childs.pipe';

describe('GetSubChildsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSubChildsPipe();
    expect(pipe).toBeTruthy();
  });
});
